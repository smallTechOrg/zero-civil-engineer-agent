# Brand assets

Drop the Zer0 Rail Agent logo here as `zero-rail-logo.png` (white line-art, transparent
background works best on the dark studio theme). It is served at `/app/brand/zero-rail-logo.png`
and rendered in the top bar by `AppShell.tsx`. Until the file is present, the header falls back
to the accent glyph automatically — nothing breaks.
