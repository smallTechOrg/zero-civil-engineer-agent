# Roadmap

---

## What This Agent Does

An agentic AI **design platform** for Indian Railways engineering — a **multi-domain (civil AND mechanical)** demonstrator that designs an IR engineering component from a natural-language request and then proof-checks its own design against IR/IRS codes, covering both the DESIGN and REVIEW halves of the IR workflow with real, verifiable artefacts. It is built on a **shared core pipeline** (extract → analyse → check → draw → 3D → review) and a **Component Registry**: each structure/component type is a first-class plug-in implementing a common interface (typed parameters, sizing/analysis, code-checks, drawing template, 3D model, proof-check rules, type-specific outputs). The agent **auto-detects the component type from the prompt** ("design a 5 m retaining wall for a cutting" → retaining-wall module) and also offers an **explicit component picker/gallery**.

The **single box culvert** is the first registered component; the **RCC cantilever retaining wall** is the second. From one prompt the platform streams a visible plan, classifies the component, extracts typed parameters, runs the selected component's deterministic engineering core, and produces: a clause-cited calculation sheet with a drill-down calc trail, a dimensioned GA drawing (genuine DXF + in-browser SVG, RDSO-style title block), an interactive 3D model (GLB + STEP), and an automatic severity-graded proof-check memo with a compliance matrix and an independent cross-check.

Every component shares one **IR-protocol review spine**: design → independent check → severity-graded proof-check memo → rule-computed verdict, LLM-narrated but numerically grounded (no number in the memo that isn't in the deterministic results). Only the **code set** differs per component (civil → IRS Bridge Rules / IRS Concrete & Steel Bridge Codes / IS 456 / IS 800; mechanical, later → RDSO specs, IS 800, welding codes). Everything runs on the presenter's laptop; the only network call is the LLM API. No mocks anywhere — the DXF opens in AutoCAD and free viewers.

## Who Uses It

- **The presenter** (primary, demo day): drives a scripted live demo for IRICEN (Indian Railways Institute of Civil Engineering, Pune) trainers and senior railway leadership. The scripted path must be flawless, first-time-right.
- **Leadership self-serve** (immediately after): senior engineers type their own requests — so guard rails (scope gate, one clarifying question, unusual-value flags, transparent failures) matter as much as the happy path.

## Core Problem Being Solved

IR bridge design and independent proof-checking (DDC → Proof Checking Consultant → CBE, tightened post-Pamban) is manual, slow, and expertise-bottlenecked. The demonstrator shows that an agent can (a) produce a genuine, code-cited design package from plain language in under a minute, and (b) act as an instant, independent, clause-cited first-pass proof-checker — the strategic sell to this audience.

## Success Criteria

- [ ] **Platform:** a new component type is added by adding ONE `src/components/<type>/` module implementing the `ComponentModule` interface and registering it — with NO change to the graph shape, API routes, DB schema, or frontend shell (proven by the retaining wall landing purely as a registry plug-in).
- [ ] **Both selection paths work:** auto-detect routes a retaining-wall prompt to the retaining-wall module and a culvert prompt to the culvert module; the picker lets the user force either, and a `coming_soon` type degrades gracefully.
- [ ] **No culvert regression:** every existing culvert unit/validation/integration/E2E test stays green after the refactor.
- [ ] Both a retaining wall AND a culvert complete a full run (all artefacts + proof-check) in **under 60 seconds** each, with artefacts streaming in as ready (calc sheet before drawing before review).
- [ ] The downloaded DXF passes ezdxf's audit cleanly and opens in free CAD viewers with correct dimensions (verified manually before demo day).
- [ ] The engine matches its named validation fixtures (V1–V4 in [irs-engine.md](capabilities/irs-engine.md#validation-fixtures--named--the-phase-2-gate-runs-these)): EUDL/CDA transcription, RDSO B-10152/R cross-check ±10%, published worked example ±5%, FE-vs-closed-form ±5%.
- [ ] Every transcribed loading/code table surfaces its source document and ACS correction-slip level in the calc sheet and UI.
- [ ] The three-act demo script (design → refine → deliberately-under-designed run caught by the proof-check) passes headless E2E against the real LLM.
- [ ] Guard rails behave: missing critical param → exactly one pointed question; "design a suspension bridge" → graceful scope statement; abnormal fill → visible flag.

## What This Agent Does NOT Do (Out of Scope)

- **Not a generic CAD tool.** The platform stays IR-specific: every design on the demo path is IR/IRS-validated and clause-cited. Non-railway or non-engineering requests get a graceful scope statement.
- Component types beyond those **registered and `status="available"`** at the current phase. The civil breadth types (plate girder, slab/T-beam, pier & abutment) are **built and available** (Expansion Phase 2, breadth-first). The mechanical domains (structural steel/fabrication, rolling-stock members, machine elements) are now **built and available** too (Expansion Phase 3, breadth-first) — so every component on the phased plan is registered and selectable, and the picker shows **no** greyed "Coming soon" previews. Deepening each breadth-first type to full culvert/retaining-wall parity remains later work.
- FOBs, multi-cell/double-line boxes, skew culverts remain unscheduled follow-ons.
- Loading standards beyond 25t Loading-2008 for the culvert (the loading layer is pluggable; DFC 32.5t is a later drop-in, not built now).
- Hydraulic design computation (vent area, HFL, afflux, scour per RBF-16) — echoed as user-supplied inputs in the proof-check, honestly marked "not verified".
- Reinforcement detailing drawings and bar-bending schedules — the GA drawing is the drawing deliverable.
- Auto-iterate-until-pass — revision is always user-triggered. (This refers to the agent-level design → review → revise loop; engine-internal check-governed sizing of AUTO-sized members is in scope — see [irs-engine.md](capabilities/irs-engine.md).)
- LLM-generated CAD/drawing code, DWG output, hosted deployment, authentication/multi-user, licensed-software integration (OpenSTAAD/OpenRail stay on the pitch slide).

## Key Constraints

- **Fixed demo date a few weeks out** — the demoable core (Phases 1–2) lands early; 3D/library polish (Phase 3) is the bonus tier and is cuttable without harming the core story.
- Runs entirely on the presenter's laptop (localhost:8001); internet used only for LLM API calls; zero paid licenses or GUI CAD processes.
- LLM = Gemini `gemini-2.5-pro` for ALL agent steps (key already in `.env` as `AGENT_GEMINI_API_KEY`).
- Full run ≤ ~60 s; UI shows the agent working the whole time (step tracker, narration, elapsed time, tokens/cost).
- Engineering must be validated against published worked examples before demo day (fixtures V1–V4); a team IR engineer and an IRICEN contact pre-review the encoded tables and drawing conventions.
- **Codes are per-component, declared by the module** (`ComponentModule.codes`), and the demo path stays clause-cited under the IR umbrella. For the **culvert** the code set is IRS-only — an IS 456/IS 800/IRC citation on a culvert output is a defect. For the **retaining wall** the module declares IRS Concrete Bridge Code + IS 456 (RCC section design) + IR track-surcharge per Bridge Rules + IRS Bridge Substructure & Foundation Code (earth-pressure/stability basis, as the pier/abutment declares for the same basis); a citation outside a component's declared `codes` set is the defect, not IS-citations per se.

---

## Phases of Development

Three phases: Phase 1 (smallest first-time-right win), two requirements phases of ≥3 capabilities each. Every slice below lists its exact owned paths — disjoint within a phase; dependencies are declared where true. All gates run against the real Gemini API via `.env` and the production DB driver (SQLite — production for this local demo).

### Phase 1 — NL → Real Drawing (the smallest first-time-right win)

- **Goal:** the full primary journey, real end-to-end: type an NL request → watch the live step tracker with narrated status and elapsed time → parameter extraction (real Gemini, incl. the one-clarifying-question and scope-gate guard rails) → deterministic IRS engine sizes the culvert (**sizing only in this phase — full loads/analysis/checks land in Phase 2**, explicitly deferred) → hand-validated parametric DXF GA drawing → SVG with pan/zoom in the browser + "Download DXF" that opens cleanly in free viewers. Session turn memory works (a follow-up "increase fill to 4 m" regenerates the drawing). Tokens/cost display live. Calc Sheet, Proof-Check, 3D, Library tabs are clearly-labelled "Coming in Phase N" stubs (never mistakable for bugs). Observability (structlog JSON per LLM call/node/run) wired from day one.
- **Capabilities delivered:** [nl-design-intake](capabilities/nl-design-intake.md) (full) · [irs-engine](capabilities/irs-engine.md) (sizing subset) · [ga-drawing](capabilities/ga-drawing.md) (full) · [session-refinement](capabilities/session-refinement.md) (turn memory + refinement regeneration; suggestions deferred to Phase 3).
- **Independent slices (parallel build units):**
  - `p1-domain-engine` (backend) — CulvertParams/BoxGeometry/Assumption/CalcStep models + sizing engine with defaults and trail. Owns: `src/domain/culvert.py`, `src/engine/__init__.py`, `src/engine/sizing.py`, `src/engine/defaults.py`, `src/engine/trail.py`, `tests/unit/engine/`. Deps: none.
  - `p1-drawing` (backend) — parametric ezdxf GA template + SVG render. Owns: `src/drawing/` (all), `tests/unit/drawing/`. Deps: `p1-domain-engine` (imports the domain models).
  - `p1-graph-llm` (backend) — graph rewrite (understand/extract/clarify/analyse/check-stub/draw/model3d-stub/review-stub/finalize/handle_error), LLM structured-output + usage extension, prompts, progress event bus, runner. Owns: `src/graph/` (all), `src/llm/client.py`, `src/llm/providers/gemini.py`, `src/prompts/` (all, incl. deleting `transform.md`), `src/observability/progress.py`, `tests/unit/graph/`, `tests/integration/`. Deps: `p1-domain-engine`, `p1-drawing` (calls their spec'd function signatures).
  - `p1-api-db` (backend) — schema migration 0002 (sessions/design_runs/artifacts/presets, drops legacy `runs`), sessions/designs/SSE/artifact endpoints incl. the designs listing (powers turn-history rehydration) and preset-read, settings additions (artifacts dir, cost rates, `llm_model` default `gemini-2.5-pro`), `.env.example` update, `pyproject.toml` deps for the phase (ezdxf dependency already pinned at scaffold as `ezdxf==1.4.4` — plain, no `[draw]` extra). Owns: `src/db/models.py`, `src/db/session.py` (if touched), `alembic/versions/0002_culvert_schema.py`, `src/api/` (all: replaces `runs.py` with `sessions.py`, `designs.py`, `presets.py`; router registration in `__init__.py`), `src/domain/api.py` (DTOs, replacing `src/domain/run.py`), `src/config/settings.py`, `.env.example`, `pyproject.toml`, `README.md`, `tests/unit/api/`, `tests/unit/db/`. Deps: `p1-graph-llm` (invokes `start_design_run`).
  - `p1-frontend` (frontend) — the full Design Studio per [ui.md](ui.md): prompt panel, step tracker, status line, Drawing tab (pan/zoom + DXF download), turn history, token/cost badge, labelled stub tabs; Playwright E2E. Owns: `frontend/` (all), `tests/e2e/`, `playwright.config.ts`, root `package.json`. Deps: none (builds against [api.md](api.md)).
- **Key surfaces / files:** as owned above; the agentic stack (graph, state, nodes, assembly) is fully wired this phase with `check`/`model3d`/`review` as labelled skip-stub nodes.
- **Gate command (run in order, from repo root):**
  ```bash
  uv run alembic upgrade head && uv run alembic current            # must print a revision
  uv run pytest tests/unit tests/integration -q                     # real Gemini key from .env
  cd frontend && pnpm install && pnpm build && cd ..
  npx playwright test tests/e2e --reporter=line                     # boots `uv run python -m src` via webServer; asserts styled render + real drawing
  ```
  The Playwright `webServer` command is exactly the documented run command (`uv run python -m src`), so the boots-via-run-command gate is covered; the E2E asserts the page is styled, submits the canonical prompt, watches the tracker advance, and asserts a real SVG drawing appears and the DXF download responds.
- **How the user tests it (handoff seed):**
  1. `cd frontend && pnpm install && pnpm build && cd ..`, then `uv run alembic upgrade head && uv run python -m src`; open `http://localhost:8001/app/`.
  2. Type: `single box culvert, 4 m clear span, 3 m height, 2.5 m cushion, BG single line, 25t loading` → Design. Watch the six-step tracker light up with the narrated plan and elapsed time; within ~30 s the Drawing tab shows a dimensioned GA sheet — pan/zoom it; click **Download DXF** and open the file in a free CAD viewer (dimensions read 4000/3000 etc.).
  3. Type: `increase the fill to 4 m` → the drawing regenerates with the 4.0 m fill dimension.
  4. Type: `design a suspension bridge` → graceful scope statement (not an error). Type: `box culvert 3 m height, 2 m cushion` → exactly one question asking for the clear span; answer `4.5 m` → drawing appears.
  5. Labelled stubs (not bugs): Calc Sheet / Proof-Check tabs say "Coming in Phase 2", 3D Model / Library say "Coming in Phase 3", one muted "suggestions coming in Phase 3" chip. Real: everything else on the path you just walked. Header shows live tokens/cost.

### Phase 2 — Engineering Core: Full IRS Engine + Calc Sheet + Proof-Check

- **Goal:** the REVIEW half becomes real. Full deterministic engine (25t Loading-2008 EUDL + CDA with cushion dispersal, all load cases, rigid-frame analysis, IRS CBC member checks) validated against the named fixtures; the clause-cited calc sheet with total drill-down streams in before the drawing; every design is automatically proof-checked (12-item checklist incl. independent anaStruct FE cross-check ±5% and DXF read-back, compliance matrix, severity-graded memo, verdict) — enabling the deliberately-under-designed demo act and the user-triggered design → review → revise loop.
- **Capabilities delivered:** [irs-engine](capabilities/irs-engine.md) (complete) · [calc-sheet](capabilities/calc-sheet.md) · [proof-check](capabilities/proof-check.md).
- **Independent slices:**
  - `p2-loading-tables` (backend) — pluggable LoadingStandard layer + transcribed 25t-2008 tables with source + ACS-level citations; fixture V1. Owns: `src/engine/loading/` (all), `tests/unit/engine/loading/`. Deps: none.
  - `p2-frame-analysis` (backend) — load-case builder + closed-form rigid-frame analysis + envelopes; domain model extensions (AnalysisResult, MemberForces); fixture V3. Owns: `src/engine/loads.py`, `src/engine/analysis.py`, `src/domain/culvert.py` (extensions), `tests/unit/engine/test_loads.py`, `tests/unit/engine/test_analysis.py`, `tests/validation/`. Deps: `p2-loading-tables`.
  - `p2-cbc-checks` (backend) — IRS CBC member checks + calc-sheet composer; fixture V2. Owns: `src/engine/checks.py`, `src/engine/calcsheet.py`, `tests/unit/engine/test_checks.py`, `tests/unit/engine/test_calcsheet.py`. Deps: `p2-frame-analysis` (AnalysisResult type).
  - `p2-fe-crosscheck` (backend) — anaStruct re-solve, diff vs closed-form, BMD/SFD SVG via matplotlib; fixture V4. Owns: `src/engine/fe_check.py`, `tests/unit/engine/test_fe_check.py`, `pyproject.toml` (adds `anastruct==1.7.0`, `matplotlib`). Deps: `p2-frame-analysis` (load-case interface).
  - `p2-proofcheck` (backend) — 12-item deterministic rules (incl. DXF read-back), memo composer + narration prompt. Owns: `src/proofcheck/` (all), `src/prompts/memo.md`, `tests/unit/proofcheck/`. Deps: `p2-cbc-checks`, `p2-fe-crosscheck` (result types).
  - `p2-graph-wiring` (backend) — analyse goes full, check/review nodes go real, integration tests for the full pipeline incl. the under-designed run. Owns: `src/graph/` (all), `tests/integration/` (updates), `README.md` (phase updates). Deps: all four slices above.
  - `p2-frontend` (frontend) — Calc Sheet tab (sections, expandable trail rows, assumptions block) and Proof-Check tab (verdict banner, memo, compliance matrix, BMD/SFD) go real; E2E additions. Owns: `frontend/` (all), `tests/e2e/`. Deps: none.
- **Gate command:**
  ```bash
  uv run pytest tests/unit tests/validation tests/integration -q    # fixtures V1–V4 + real-Gemini full pipeline
  cd frontend && pnpm build && cd ..
  npx playwright test tests/e2e --reporter=line                     # expands a calc row; asserts verdict banner + matrix render
  ```
- **How the user tests it:** run the canonical prompt → the Calc Sheet tab fills first (click any number: formula + inputs expand; assumptions block on top; ACS level visible on loading lines), then Drawing, then the Proof-Check tab: green "Recommended for approval", memo, 12-row matrix, BMD/SFD with the FE agreement figure. Then the demo money-shot: `same culvert but make the top slab only 200 mm` → amber warning at extraction, FAIL rows in the calc sheet, red "Return for revision" verdict naming the thin slab; type `increase top slab to 450 mm` → verdict recovers. Stubs remaining: 3D Model, Library, suggestion chips (all "Coming in Phase 3").

### Phase 3 — Demo Completeness: 3D + Library + Refinement Suggestions

- **Goal:** the remaining stubs go real: interactive 3D model (GLB viewer + STEP download) from the same parameters, the browsable design library (audit trail + run replay + presets editing), and post-run refinement suggestion chips — plus the cost/polish pass that makes self-serve smooth.
- **Capabilities delivered:** [model-3d](capabilities/model-3d.md) · [design-library](capabilities/design-library.md) · [session-refinement](capabilities/session-refinement.md) (complete: suggestions).
- **Independent slices:**
  - `p3-model3d` (backend) — build123d parametric solid → GLB + STEP, geometry-agreement tests. Owns: `src/model3d/` (all), `tests/unit/model3d/`, `pyproject.toml` (adds `build123d==0.11.1`). Deps: none.
  - `p3-library-api` (backend) — preset editing (PUT) with range validation + library-query polish (pagination/filters on the existing listing). Owns: `src/api/presets.py`, `src/api/designs.py` (listing additions only), `tests/unit/api/` (additions). Deps: none.
  - `p3-graph-suggestions` (backend) — model3d node goes real (non-fatal policy), finalize gains the suggestions call. Owns: `src/graph/` (all), `src/prompts/suggest.md`, `tests/integration/` (additions), `README.md` (phase updates). Deps: `p3-model3d` (imports the generator).
  - `p3-frontend` (frontend) — 3D tab (model-viewer, STEP download), Library tab (table, run replay, presets editor, empty state), suggestion chips; E2E additions. Owns: `frontend/` (all), `tests/e2e/`. Deps: none.
- **Gate command:**
  ```bash
  uv run pytest tests/unit tests/validation tests/integration -q
  cd frontend && pnpm build && cd ..
  npx playwright test tests/e2e --reporter=line                     # asserts 3D viewer loads, library lists runs, chips render
  ```
- **How the user tests it:** run the canonical prompt → 3D Model tab shows the rotating culvert (orbit/zoom); **Download STEP** and open it in FreeCAD. After completion, 2–3 suggestion chips appear — click one, it fills the prompt box; submit and everything regenerates. Open Library: every run from all phases listed with verdict chips and costs; click an old run — its drawing/calc/check replay exactly; edit the default preset's cover 50 → 40 mm and confirm old runs still show 50 while a new run shows 40. Nothing is a stub anymore.

---

## Expansion Phases (platform evolution — culvert Phases 1–3 above are DONE)

Phases 1–3 delivered the full culvert (design + proof-check + 3D + library). The expansion turns that single-structure tool into the multi-domain platform. **Expansion Phase 1 is the only phase built now.** All gates run against the real Gemini key in `.env`; live servers run on **port 8004** (`E2E_PORT=8004`) so a live 8001 is never touched.

### Expansion Phase 1 — Shared Framework + RCC Cantilever Retaining Wall (second registered component, FULL parity)

- **Goal:** extract the shared **Component Registry + component interface**, re-register the box culvert against it with **zero regression**, and add the **RCC cantilever retaining wall** as the second fully-featured component — NL/picker selection, deterministic engine (earth pressure + stability + RCC section design), GA drawing (DXF+SVG) + 3D (GLB+STEP), and the SAME IR-protocol proof-check (severity-graded memo + verdict) — with the whole existing UX (step tracker, narration, tokens/cost, calc drill-down, library, presets, refinement) working for BOTH types and a component picker/gallery exposing the roadmap via greyed "Coming soon" stubs.
- **Capabilities delivered:** [component-registry](capabilities/component-registry.md) (new) · [retaining-wall](capabilities/retaining-wall.md) (new) · plus every existing capability generalised to dispatch by component type (nl-design-intake, irs-engine, ga-drawing, calc-sheet, proof-check, model-3d, session-refinement, design-library).
- **The concrete agreed interface** (so slices b/c/d parallelise with a on the spec, not on each other's code) is defined in [architecture.md](architecture.md#the-component-interface-srccomponentsbasepy--the-concrete-contract-slices-build-against). Slices b/c/d import from `src/components/base.py` and the retaining-wall `params.py`/geometry model whose **field lists are fixed in [capabilities/retaining-wall.md](capabilities/retaining-wall.md)**.
- **Independent slices (disjoint owned paths):**
  - **(a) core-framework + registry + culvert re-registration** (backend). **Owns:** `src/components/__init__.py`, `src/components/base.py`, `src/components/registry.py`, `src/components/culvert/` (all — the adapter wrapping the unchanged `src/engine`, `src/drawing`, `src/model3d`, `src/proofcheck`), refactors of `src/graph/state.py` (adds `component_type`), `src/graph/nodes.py` (dispatch via `registry.get`), `src/graph/edges.py`, `src/graph/agent.py` (shape unchanged), `src/graph/runner.py` (thread `component_type` through initial state), `src/prompts/understand.md` + `src/prompts/extract.md` (classify + dynamic-schema instructions), `tests/unit/components/test_registry.py`, `tests/unit/components/test_culvert_module.py`, updates to `tests/unit/graph/`. **Deps:** none. Publishes the interface everything else builds against.
  - **(b) retaining-wall engine** (backend). **Owns:** `src/components/retaining_wall/params.py`, `sizing.py`, `analysis.py` (Rankine/Coulomb active & passive earth pressure incl. IR track surcharge + optional surcharge), `checks.py` (overturning/sliding/bearing stability factors + RCC stem/heel/toe section design per IS 456 / IRS Concrete Bridge Code, clause-cited), `calcsheet.py`, `summary.py` (type-specific stability summary: FoS overturning/sliding, max bearing vs SBC), `tests/unit/components/retaining_wall/engine/`, `tests/validation/test_rw_worked_example.py` (published worked example, ±5%). **Deps:** (a) — imports `base.py` result types + interface. Builds against the spec'd interface + its own param/geometry field lists.
  - **(c) retaining-wall drawing + 3D** (backend). **Owns:** `src/components/retaining_wall/drawing.py` (parametric ezdxf GA: cross-section + plan, dimensions, RDSO-style title block → SVG + DXF), `src/components/retaining_wall/model3d.py` (build123d solid → GLB + STEP), `tests/unit/components/retaining_wall/test_drawing.py`, `test_model3d.py`. **Deps:** (a) interface + (b)'s `RetainingWallParams`/`RetainingWallGeometry` field lists (fixed in the capability doc — so builds in parallel).
  - **(d) retaining-wall proof-check + module assembly** (backend). **Owns:** `src/components/retaining_wall/proofcheck.py` (retaining-wall-specific checklist + severity-graded memo through the SAME IR checking-engineer workflow; independent cross-check = recomputed stability factors, plus anaStruct where a stem-as-cantilever frame applies), `src/components/retaining_wall/module.py` (the `RetainingWallComponent` wiring b+c+d and calling `registry.register()`), `src/components/retaining_wall/__init__.py`, `src/prompts/rw_memo.md`, `tests/unit/components/retaining_wall/test_proofcheck.py`, `tests/unit/components/retaining_wall/test_module.py`. **Deps:** (a), (b), (c) — assembles the full module; imports are call-time so it builds in parallel against the spec'd signatures.
  - **(e) frontend** (frontend). **Owns:** `frontend/` (all), `tests/e2e/`. Component picker/gallery (Box Culvert + Retaining Wall available; greyed "Coming soon" for plate girder, slab/T-beam, pier & abutment, and a "Mechanical" domain group), auto-detect surfacing (shows the classified type + lets the user switch), type-aware prompts, type-aware artefact panels (retaining-wall **stability summary** panel; culvert panels unchanged), library/presets/refinement chips/cost/token/step-tracker all working for BOTH types. Stubs clearly labelled so they never read as bugs. **Deps:** none (builds against [api.md](api.md) + [ui.md](ui.md)).
- **Gate command (run in order from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run alembic upgrade head && uv run alembic current
  uv run pytest tests/unit tests/validation tests/integration -q       # real Gemini; registry + RW engine + NO culvert regression
  cd frontend && pnpm install && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line          # boots `uv run python -m src` on 8004
  ```
  The E2E asserts, on a styled render: (1) the canonical **culvert** prompt still completes with a real SVG GA + passing/graded proof-check (regression); (2) a **retaining-wall** prompt ("design a 5 m high RCC cantilever retaining wall, SBC 200 kN/m², BG single line track surcharge") auto-routes to the retaining-wall module, streams a real GA SVG, a stability-summary panel with FoS overturning/sliding + bearing-vs-SBC, and a proof-check verdict; (3) `ga.dxf` for the retaining wall round-trips via `ezdxf.readfile`; (4) the retaining-wall `model.glb` is a valid non-empty GLB. The integration suite includes a backend assertion that `ezdxf.readfile` opens the RW DXF and that the GLB parses.
- **How the user tests it (handoff seed):**
  1. `cd frontend && pnpm install && pnpm build && cd ..`, then `AGENT_PORT=8004 uv run alembic upgrade head && AGENT_PORT=8004 uv run python -m src`; open `http://localhost:8004/app/`.
  2. **Picker:** the gallery shows Box Culvert and Retaining Wall as available, with greyed "Coming soon" cards (plate girder, slab/T-beam, pier & abutment, Mechanical). Pick **Retaining Wall** → type-aware prompt hint appears. Submit `5 m high RCC cantilever retaining wall, SBC 200 kN/m², BG single-line track surcharge, backfill φ 30°`. Watch the tracker; a GA drawing (section + plan, RDSO title block) appears — download DXF, open in a free viewer; the **Stability** panel shows FoS overturning / sliding and max bearing vs SBC; the Proof-Check tab shows a verdict + memo.
  3. **Auto-detect:** in the prompt box (no picker) type `design a retaining wall for a 4 m cutting` → the agent classifies it as a retaining wall and shows the classified type.
  4. **Regression:** run the canonical culvert prompt → identical behaviour to before (GA, calc sheet, proof-check, 3D, library entry).
  5. **Stubs (not bugs):** greyed component cards say "Coming in a later phase"; everything on the two working types is real.

### Expansion Phase 2 — Civil Breadth: Plate Girder + Slab / T-beam Superstructure + Pier & Abutment  ✅ DELIVERED / BUILT

**Status: BUILT (breadth-first).** The three components are registered on the existing interface and are `status="available"`: NL/picker → typed params → GA drawing + core code-checks + IR-protocol proof-check + 3D each work end-to-end. Their gallery cards move from greyed "Coming soon" to **available**. Full culvert/retaining-wall-level parity deepening (every load case, reinforcement/connection design, seismic/wind cases, advanced deck distribution) remains explicit **later work**, not part of this breadth-first delivery.

- **Goal:** register three more civil components **breadth-first** (NL/picker → typed params → GA drawing + core code-checks + IR-protocol proof-check + 3D), each as an independent `src/components/<type>/` plug-in; they then deepen toward culvert/retaining-wall parity in follow-up work. Each reuses the shared core and review spine; each declares its own code set (plate girder → IRS Steel Bridge Code / IS 800; slab & T-beam → IRS Concrete Bridge Code / IS 456; pier & abutment → IRS Bridge Substructure & Foundation Code).
- **Capabilities delivered:** [plate-girder](capabilities/plate-girder.md) · [slab-tbeam](capabilities/slab-tbeam.md) · [pier-abutment](capabilities/pier-abutment.md) (each a new component module; ≥3 capabilities).
- **Independent slices:** one module-directory slice per component type (`src/components/plate_girder/`, `src/components/slab_tbeam/`, `src/components/pier_abutment/`), each disjoint; one frontend slice (gallery cards go available, type-aware panels). **Deps:** the Phase-1 interface (stable).
- **Gate command:** `uv run pytest tests/unit tests/validation tests/integration -q && cd frontend && pnpm build && cd .. && E2E_PORT=8004 npx playwright test tests/e2e --reporter=line` — E2E exercises one prompt per new type plus culvert + retaining-wall regression.
- **How the user tests it:** pick or describe each new type; a real GA drawing + core checks + proof-check verdict appears; previously-built types unchanged.

### Expansion Phase 3 — Mechanical Domain: Structural Steel / Fabrication + Rolling-Stock Members + Machine Elements  ✅ DELIVERED / BUILT

**Status: BUILT (breadth-first).** The three mechanical components — structural steel / fabrication member (IS 800 / IS 816), rolling-stock member (RDSO Specifications / IS 800) and machine element (Machine Design Code / IS 816) — are registered on the SAME `ComponentModule` interface and IR-protocol review spine and are `status="available"`: NL/picker → typed params → mechanical GA drawing (with weld symbols) + core code-checks + IR-protocol proof-check + 3D each work end-to-end. Their gallery cards move from greyed "Coming soon" to **available**, so the gallery now shows **8 available components and 0 coming-soon previews** — the whole phased roadmap is delivered, proving the abstraction is not civil-specific. Full culvert/retaining-wall-level parity deepening (every load case, fatigue / connection / GD&T detailing, deeper code coverage) remains explicit **later work**, not part of this breadth-first delivery.

- **Goal:** prove the abstraction is **not civil-specific** by registering mechanical components under the same interface and review spine, differing only in codes (IS 800 for structural steel, RDSO specs for rolling-stock members, standard machine-element codes) and drawing/doc conventions (mechanical/assembly drawings with **weld symbols** and GD&T from hand-validated parametric templates). Breadth-first (NL → mechanical drawing + core checks + proof-check), then deepen.
- **Capabilities delivered:** [structural-steel-member](capabilities/structural-steel-member.md) · [rolling-stock-member](capabilities/rolling-stock-member.md) · [machine-element](capabilities/machine-element.md) (≥3 capabilities).
- **Independent slices:** one module-directory slice per mechanical type (each disjoint `src/components/<type>/`); a mechanical-drafting helper is added **only if** `ezdxf` templates prove insufficient (decided at that phase, not now); one frontend slice (Mechanical domain group goes available). **Deps:** the Phase-1 interface + the drawing-convention hooks.
- **Gate command:** as Phase 2, with E2E covering one mechanical prompt (weld-symbol drawing renders, IS 800 / RDSO checks + proof-check verdict) plus civil regression.
- **How the user tests it:** switch to the Mechanical domain in the gallery, design a steel member or machine element; a mechanical drawing with weld symbols + a code-checked proof-check verdict appears; all civil types still work.

---

## Component Addition — M-00004 Standard Box Culvert (RDSO)

A **ninth registered component** added on the unchanged interface — `m00004_box_culvert`, "M-00004 Standard Box Culvert (RDSO)", domain `civil`, codes `["RDSO/M-00004", "IRS Concrete Bridge Code"]`. Unlike the load-engineered `box_culvert`, this component is **standard-driven**: it reproduces the published RDSO/M-00004 standard — the user feeds parameters through a **typed form**, a deterministic catalogue lookup supplies thickness / haunch / reinforcement, and the component emits every artefact for the entered box (2D, 3D **and** a M-00004-styled PDF sheet with reinforcement). Because parameters are typed, the intake **bypasses the LLM understand/extract nodes** (zero LLM cost on intake). Full capability spec: [m00004-box-culvert.md](capabilities/m00004-box-culvert.md). Gates run against the real Gemini key in `.env`; live servers run on **port 8004** (`E2E_PORT=8004`).

> **Reinforcement lands fully in Phase 1 (single phase).** The catalogue extension is small (15 configs × 12 bar marks = 180 provisional numbers) and the bar layout is a fixed parametric geometry — deterministic, no analysis, no LLM. It fits one phase cleanly, so this is a **single component-addition phase** delivering the picker → form → all three output kinds **with** the a1..h reinforcement + schedule table, not split across two phases. This meets the stated target (reinforcement included) and is the honest smallest first-time-right win.

### Phase 1 (only phase) — Standard Box → all outputs with reinforcement

- **Goal:** in the studio, pick **M-00004 Standard Box Culvert (RDSO)** → a parameter form appears (clear span, clear height, cushion/fill, surcharge, formation width, side slope, grades) → enter concrete numbers → **Design** → the deterministic pipeline selects the nearest standard config, builds the standard box at the entered opening, and streams three downloadable output kinds: **2D** (`ga.dxf` + `ga.svg`, dimensioned concrete GA with RDSO-style title block), **3D** (`model.step` + `model.glb`, box + haunches + wing walls + apron + curtain walls), and the **M-00004-style PDF sheet** (`m00004_sheet.pdf`) with a dimensioned cross-section, **reinforcement bars a1..h drawn in position**, a reinforcement schedule table (mark → dia @ spacing), a notations glossary and the standard notes. Every catalogue-derived thickness/haunch/bar value carries a **"PROVISIONAL — verify against RDSO/M-00004"** marking. Intake makes **zero LLM calls** (params-direct); the review-memo narration is the only (optional, grounded) LLM call, consistent with the platform.
- **Capability delivered:** [m00004-box-culvert](capabilities/m00004-box-culvert.md). (An addition-phase capability against the existing platform — one self-contained component module, like the retaining wall was; the existing "≥3 capabilities per phase" shape applied to greenfield requirements phases, not to a single-component addition.)
- **Independent slices (disjoint owned paths):**
  - **(a) component engine + catalogue + geometry + drawing + 3D + PDF sheet** (backend). **Owns:** the entire new module directory `src/components/m00004_box_culvert/` — `__init__.py`, `params.py` (`M00004Params` / `M00004Geometry`), `catalog.json` (pilot subset copied + extended with the provisional a1..h bar schedule), `catalog.py` (loader + deterministic `select_config`), `sizing.py` (config selection → geometry + thickness/haunch/bars + assumptions/trail), `analysis.py` (minimal standard-basis record — no load analysis), `checks.py` (standard-conformance rows, all PROVISIONAL), `calcsheet.py`, `reinforcement.py` (deterministic a1..h positions), `drawing.py` (ezdxf GA section + part-plan → `ga.dxf` + `ga.svg` via the shared `drawing.svg_render`), `pdfsheet.py` (reportlab → `m00004_sheet.pdf`), `model3d.py` (build123d solid → `model.glb` + `model.step`; **reuses the pilot geometry approach, reimplemented in build123d — NOT cadquery**), `proofcheck.py` (thin standard-conformance memo + PROVISIONAL verdict on the shared IR spine), `summary.py` (`type_summary`), `module.py` (`M00004BoxCulvertComponent` + `register()`); **plus** all tests under `tests/unit/components/m00004_box_culvert/`. **Deps:** none — builds against the fixed interface (`src/components/base.py`) and the param/geometry field lists fixed in [m00004-box-culvert.md](capabilities/m00004-box-culvert.md); imports no existing component engine.
  - **(b) backend wiring — params-direct path + whitelist + DTO + registry import + deps** (backend). **Owns every shared-file edit:** `src/domain/api.py` (add `params: dict | None = None` to `DesignSubmitRequest`), `src/api/designs.py` (add `m00004_sheet.pdf` → `("application/pdf","inline")` to `ARTIFACT_FILES`; validate + thread `params` in `submit_design`, emitting `422 PARAMS_INVALID` / `422 PARAMS_REQUIRED`; add an `m00004` branch to `_params_summary`), `src/graph/state.py` (add `params_direct: bool`), `src/graph/runner.py` (`start_design_run(..., params=None)` seeds `params`/`component_type`/`params_direct`), `src/graph/edges.py` (add `route_entry` conditional-entry router), `src/graph/nodes.py` (add the `seed_params` node; add `m00004_sheet` to `_ARTIFACT_MIME`; extend the `draw` node to also emit any artefact keys the module returns beyond `_ARTIFACT_ORDER` — backward-compatible), `src/graph/agent.py` (replace the fixed `set_entry_point("understand")` with a conditional entry `START → route_entry → {understand, seed_params}` and wire `seed_params → analyse`; graph shape unchanged for every NL run), `src/components/__init__.py` (import `m00004_box_culvert`), `pyproject.toml` (add `reportlab>=4.0,<5`); **plus** test additions under `tests/unit/graph/`, `tests/unit/api/`, `tests/integration/`. **Deps:** (a) at integration/live-run time (dispatches to the module and validates against its `param_model`); builds in parallel against the fixed param field list + interface for unit tests.
  - **(c) frontend — picker entry + parameter form + artefact panels (incl. PDF)** (frontend). **Owns:** `frontend/` (new `frontend/src/components/M00004ParamForm.tsx`; edits to `frontend/src/app/page.tsx` to reveal the form when the M-00004 card is picked and submit typed `params`; `frontend/src/lib/api.ts` to send `params`; `frontend/src/lib/types.ts` for the params-submit + PDF artefact kind; `frontend/src/components/ArtefactTabs.tsx` + `DrawingViewer.tsx` for the "Open M-00004 sheet (PDF)" affordance that opens `m00004_sheet.pdf` inline / downloads it) and the Playwright spec under `tests/e2e/`. **Deps:** none — builds against [api.md](api.md) + [ui.md](ui.md) and the fixed param field list; the live E2E in the gate exercises the full stack once (a)+(b) land.
- **Key surfaces / files:** as owned above. Every shared-file edit is owned by **exactly one** slice — (b) owns all of `src/api/`, `src/domain/api.py`, `src/graph/*`, `src/components/__init__.py`, `pyproject.toml`; (c) owns all of `frontend/` + `tests/e2e/`; (a) owns only the new module dir + its unit tests. No two generators touch the same file.
- **Gate command (run in order from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run alembic upgrade head && uv run alembic current
  uv run pytest tests/unit tests/validation tests/integration -q       # real Gemini; M-00004 engine + params-direct wiring + NO regression
  cd frontend && pnpm install && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line          # boots `uv run python -m src` on 8004
  ```
  The E2E asserts, on a styled render: (1) picking the **M-00004** card reveals the parameter form; (2) submitting span 4 / height 4 / fill 2 / surcharge 0 streams a real GA SVG, a type-summary panel showing the selected config + PROVISIONAL flags, and a valid `m00004_sheet.pdf` reachable inline; (3) `ga.dxf` round-trips via `ezdxf.readfile`; (4) `model.glb` is a valid non-empty GLB; (5) the canonical **culvert** and a **retaining-wall** prompt still complete unchanged (regression). The integration suite asserts config selection (`F2_4x4` for 4×4/2 m), the a1..h schedule is present, the PDF is a non-empty `application/pdf`, and the run makes zero LLM intake calls.
- **How the user tests it in the browser (handoff seed):**
  1. `cd frontend && pnpm install && pnpm build && cd ..`, then `AGENT_PORT=8004 uv run alembic upgrade head && AGENT_PORT=8004 uv run python -m src`; open `http://localhost:8004/app/`.
  2. **Picker → form:** in the left-rail gallery pick **M-00004 Standard Box Culvert (RDSO)** (Civil group). A parameter form appears — enter **clear span 4 m, clear height 4 m, cushion/fill 2 m, surcharge 0** (leave formation width 6.85 m and side slope 2 as defaults) → click **Design**. The step tracker lights up with Understand + Extract already marked done ("entered via the parameter form") and streams straight to Analyse → Check → Draw → Review.
  3. **Three outputs:** the **Drawing** tab shows the dimensioned concrete GA (pan/zoom); click **Download DXF** and open it in a free viewer (span reads 4000, height 4000). Click **Open M-00004 sheet (PDF)** → the PDF opens with the title block, dimensioned cross-section, the **reinforcement bars a1..h drawn in position** with `mark : dia @ spacing` tags, the reinforcement schedule table, notations glossary and notes — every catalogue value stamped **PROVISIONAL**. The **3D Model** tab shows the box with haunches + wing walls + apron + curtain walls; **Download STEP** opens in FreeCAD. The **Stability / Type Summary** panel shows the selected config `F2_4x4`, thickness 500 mm, haunch 450 mm, derived barrel length, and the PROVISIONAL flags.
  4. **Out-of-catalogue:** submit span 7 / height 7 / fill 3 / surcharge 10 → the design still completes, and the type summary + PDF carry explicit nearest-config / extrapolation PROVISIONAL notes (fill, box size and surcharge each flagged) — not an error.
  5. **What is REAL vs PROVISIONAL/stub:** REAL — the picker, the form, the deterministic config selection, the concrete geometry, the 2D GA (DXF/SVG), the 3D solid (STEP/GLB), the PDF sheet layout and the bar **positions**. PROVISIONAL (clearly marked, not a bug) — every thickness / haunch / bar **dia @ spacing number** (digitized subset) and any out-of-catalogue selection. Regression check: the canonical culvert prompt and a retaining-wall prompt behave exactly as before.

### Phase 2 — Full RDSO/M-00004 GA sheet: all six drawings + STEP parts + composed sheet + zip

- **Goal:** turn the single-view M-00004 output into the **full standard GA sheet**. From the same picker → form → Design, the run now streams **one DXF+SVG per diagram** — Sectional Elevation at X-Y, Cross-Section of the RCC box (a1..h bars), Plan, Section of Curtain/Drop Wall, Typical Details at A & B, Return Wall, plus the bar-bending SHAPE table, notations glossary, notes block and B×B haunch table — **genuinely-3D STEP parts** (full assembly + box + curtain/drop wall + return wall, each volume-verified), and, at the review stage, ONE **composed PDF laid out like the real M-00004 sheet** (`m00004_ga_sheet.pdf`) plus a **`.zip` bundle** of every per-diagram DXF + STEP. Materials defaults change to **Fe415** steel and **derived M-35 concrete** (M40 for very-severe exposure). 100% deterministic parametric (ezdxf/build123d/matplotlib+reportlab, NO LLM CAD); every value from `M00004Geometry`/`M00004Params`; every new surface stamped PROVISIONAL / NOT-FOR-CONSTRUCTION. **All Phase-1 artefacts keep working unchanged.** Full scope + the normative new-field/constant/artefact tables: [m00004-box-culvert.md → Phase 2](capabilities/m00004-box-culvert.md#phase-2--full-rdsom-00004-ga-sheet-all-six-drawings--step-parts--composed-sheet--zip).
- **Because params/geometry are single-source, the foundation slice (a) publishes the fixed new fields/constants/material defaults; every diagram/3D/compose slice builds against the FIXED capability-spec tables, not each other's code.** No two slices edit the same file.
- **Independent slices (disjoint owned paths; deps declared):**
  - **(a) foundation — params/geometry/materials/sizing** (backend). **Owns:** `src/components/m00004_box_culvert/params.py` (new `M00004Params` fields — `concrete_grade: ConcreteGrade|None=None`, `steel_grade` default `Fe415`, new `exposure: ExposureCondition` enum; the ten new engine constants; the fifteen new `M00004Geometry` fields), `sizing.py` (populate the new geometry fields + `resolve_concrete_grade()` derivation + PROVISIONAL assumptions), `catalog.py`, `catalog.json`, `analysis.py`, `checks.py`, `calcsheet.py`, `summary.py`, **and `src/domain/culvert.py`** (add `ConcreteGrade.M40`, additive/regression-safe), plus `tests/unit/components/m00004_box_culvert/` for these. **Deps:** none — publishes the interface the rest build against.
  - **(b) 2D diagrams** (backend). **Owns:** `src/components/m00004_box_culvert/drawing.py` (refactored into the aggregator that keeps returning `ga_dxf`/`ga_svg`/`m00004_sheet` and adds every `<diagram>_dxf`/`<diagram>_svg` pair), the new `src/components/m00004_box_culvert/drawings/` subpackage (`__init__.py`, `elevation.py`, `cross_section.py`, `plan.py`, `curtain_wall.py`, `typical_details.py`, `return_wall.py`, `bar_shape_table.py`, `notations.py`, `notes.py`, `haunch_table.py`), `reinforcement.py`, plus `tests/unit/components/m00004_box_culvert/drawings/`. Imports the stable `pdfsheet.generate_sheet` (owned by d; unchanged signature) for the Phase-1 sheet. **Deps:** (a) field list (spec).
  - **(c) 3D / STEP** (backend). **Owns:** `src/components/m00004_box_culvert/model3d.py` (refactor to build+export the assembly/box/curtain-drop/return-wall solids as STEP each, keep the fused `model.glb`/`model.step`, self-verify each closed-form sub-volume, return the six keys) + `tests/unit/components/m00004_box_culvert/test_model3d.py`. **Deps:** (a) field list (spec).
  - **(d) compose + zip** (backend). **Owns:** `src/components/m00004_box_culvert/compose.py` (matplotlib + ezdxf-matplotlib backend → `m00004_ga_sheet.pdf` from on-disk per-diagram DXFs + tables/notes/title from geometry), `src/components/m00004_box_culvert/bundle.py` (stdlib `zipfile` → `m00004_bundle.zip` of on-disk per-diagram DXF + STEP), and **`pdfsheet.py`** (ADD `generate_composed_sheet` helpers if used; keep `generate_sheet` signature stable), plus `tests/unit/components/m00004_box_culvert/test_compose.py`, `test_bundle.py`. **Deps:** (a),(b),(c) — composes their on-disk outputs (builds against the FIXED filenames in the spec; integrates at the gate).
  - **(e) backend wiring — module hook + whitelist + mime + emit-loops** (backend). **Owns every shared-file edit:** `src/components/m00004_box_culvert/module.py` (add the `compose(params, geometry, out_dir, run_id)` hook delegating to `compose.py`/`bundle.py`; `draw()`/`model3d()` already pass their dicts through unchanged), `src/api/designs.py` (add the 26 new filenames to `ARTIFACT_FILES` per the capability table), `src/graph/nodes.py` (add the 26 new kinds to `_ARTIFACT_MIME`; change the `model3d` node to loop over **whatever keys `module.model3d()` returns**, backward-compatibly; add the guarded `getattr(module,"compose",None)` compose emission in the `review` node inside its own non-fatal try/except), plus test additions under `tests/unit/graph/`, `tests/unit/api/`, `tests/integration/`. **Does NOT touch `src/domain/culvert.py`** (owned by (a)) or `pyproject.toml` (no new dep — reportlab/ezdxf/build123d/matplotlib already present). **Deps:** (a),(b),(c),(d).
  - **(f) frontend — full-sheet review/download UI** (frontend). **Owns:** `frontend/` (a diagram gallery/list in the Drawing/Review surface that lists and downloads each new per-diagram DXF/SVG + each STEP part + the composed `m00004_ga_sheet.pdf` (inline) + the `m00004_bundle.zip`, all with PROVISIONAL labels; the composed-sheet + zip download affordances) + `tests/e2e/`. **Deps:** none — builds against [api.md](api.md) + the fixed artefact-filename table; live integration at the gate once (a)+(b)+(c)+(d)+(e) land.
- **Wave order:** all slices build in parallel against the FIXED capability-spec interfaces (field lists, constants, artefact kind→filename→mime). True integration deps: **wave 1** = (a), (b), (c), (f) each against the spec; **wave 2** = (d) composes (a)/(b)/(c) outputs; **wave 3** = (e) wires module hook + whitelist + mime + emit-loops and runs the integration/live tests. No two slices share a file.
- **Key surfaces / files:** as owned above — (a) owns `params.py`/`sizing.py`/`catalog*`/`analysis.py`/`checks.py`/`calcsheet.py`/`summary.py`/`domain/culvert.py`; (b) owns `drawing.py`/`drawings/`/`reinforcement.py`; (c) owns `model3d.py`; (d) owns `compose.py`/`bundle.py`/`pdfsheet.py`; (e) owns `module.py`/`api/designs.py`/`graph/nodes.py`; (f) owns `frontend/`+`tests/e2e/`.
- **Gate command (run in order from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run alembic upgrade head && uv run alembic current
  uv run pytest tests/unit tests/validation tests/integration -q       # real Gemini; M-00004 full GA sheet + STEP parts + compose/zip + NO regression
  cd frontend && pnpm install && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line          # boots `uv run python -m src` on 8004
  ```
  The E2E asserts, on a styled render: (1) submitting span 4 / height 4 / fill 2 / surcharge 0 streams the six per-diagram SVGs (elevation, cross-section, plan, curtain/drop wall, typical details, return wall) plus the bar-bending/notations/notes/haunch blocks, each downloadable as DXF; (2) `m00004_ga_sheet.pdf` (composed) is reachable inline and `m00004_bundle.zip` downloads; (3) `elevation.dxf`, `cross_section.dxf`, `plan.dxf`, `return_wall.dxf`, `curtain_wall.dxf` each round-trip via `ezdxf.readfile`; (4) `assembly.step`/`box.step`/`curtain_wall.step`/`return_wall.step` each open and each part's built volume matches its closed-form sub-volume; (5) materials — default steel `Fe415`, resolved concrete `M35` (and `M40` under very-severe exposure); (6) the Phase-1 `ga.dxf`/`ga.svg`/`model.glb`/`model.step`/`m00004_sheet.pdf` still emit, and the canonical **culvert** and a **retaining-wall** prompt still complete unchanged (regression, incl. `ConcreteGrade.M40` additive with no other-component impact).
- **How the user tests it in the browser (handoff seed):**
  1. `cd frontend && pnpm install && pnpm build && cd ..`, then `AGENT_PORT=8004 uv run alembic upgrade head && AGENT_PORT=8004 uv run python -m src`; open `http://localhost:8004/app/`.
  2. **Picker → form:** pick **M-00004 Standard Box Culvert (RDSO)**; enter **clear span 4 m, clear height 4 m, cushion 2 m, surcharge 0** (leave formation width / side slope defaults; concrete grade left on **Auto**, exposure **Severe**) → **Design**.
  3. **The full sheet:** the **Drawing/Review** surface now lists **all six drawings** — open each SVG (Sectional Elevation with HFL line, formation level, 1-in-100 bed slope and 300 stone pitching; Cross-Section with a1..h bars; Plan with 75-dia weep holes @ 1000 c/c and splayed wing walls; Curtain/Drop-Wall section; Typical Details A & B; Return Wall) plus the bar-bending SHAPE table, notations glossary, notes and B×B haunch table. Download any as **DXF** and open in a free viewer. Title block reads **Fe415 / M35**.
  4. **3D parts:** the 3D tab shows the assembly; **Download STEP** now offers the **full assembly** plus **box**, **curtain/drop wall** and **return wall** individually — each opens in FreeCAD.
  5. **Composed sheet + bundle:** at Review, click **Open M-00004 GA sheet (PDF)** → `m00004_ga_sheet.pdf` opens inline with the six drawings positioned as on the real M-00004 sheet + notations + notes + bar-bending + haunch tables + material specs + RDSO title block + the bold **PROVISIONAL — NOT FOR CONSTRUCTION** strip. Click **Download bundle (.zip)** → `m00004_bundle.zip` contains every per-diagram DXF + every STEP part.
  6. **Very-severe exposure:** re-run with **exposure = Very severe** → title block + notes now read **M40** (derived); everything else unchanged.
  7. **What is REAL vs PROVISIONAL/stub:** REAL — every diagram, STEP part, the composed sheet layout and the zip. PROVISIONAL (clearly marked, not a bug) — every thickness/haunch/bar number, the HFL/return-wall/drop-wall assumption values, and any out-of-catalogue selection. Regression: the Phase-1 M-00004 outputs, the canonical culvert prompt and a retaining-wall prompt behave exactly as before.

---

**After the platform (not scheduled):** DFC 32.5t loading drop-in, full RDSO/M-00004 annexure digitization + verified reinforcement, reinforcement drawings + BBS for the load-engineered types, deepening each breadth-first type to full parity — follow-ons beyond this POC.

---

## Phases of Development

> **Phase 4 Redesign — enterprise UX/IA overhaul.** This section plans the UX/UI redesign of the EXISTING platform (branch `v0.4-phase-4-redesign`). It is **not** a new idea and **not** a new component/capability: the backend engineering core, the 8 registered component types, the agent graph, the extract→analyse→check→draw→3D→review pipeline, all six outputs, every API endpoint and the DB schema stay functionally the same. The work reorganises the SAME elements into a lifecycle-oriented, extensible, enterprise-grade experience and fixes concrete UI mismatches. It is **primarily a frontend effort** (see [ui.md](ui.md) for the new IA and [architecture.md](architecture.md#ux-redesign-phase-4-redesign--ia-only-no-backend-rearchitecture) for why no backend rearchitecture is needed). The three redesign phases below are **appended to**, and do not replace, the Expansion Phases above. All gates run against the real Gemini key in `.env`; live servers run on **port 8004** (`E2E_PORT=8004`) so a live 8001 is never touched.

**What the redesign delivers (across the three phases):** design-as-first-class-record with a replayable Design Records rail; a Define → Design → Review **Stage Rail** with visibly-coming Simulate / Test / Approve stubs; a **prompt-first + roomy Civil/Mechanical gallery** entry with "let the agent decide" prominent; a per-design **Overview** (verdict + key numbers + drawing thumbnail + cost) that **removes the always-on, frequently-blank generic "Stability" tab**; design-level surfacing of run/refine history, code/standards traceability, status-at-a-glance chips (Draft / Reviewed ✓ / Needs revision ✗) and per-run + session token/cost; the **refined technical dark studio** shell; and the concrete fixes — platform-level title (was hardcoded "IR Box Culvert Design & Proof-Check Agent" / hero "Design a single-cell RCC box culvert") and **no tab-yank** (active view was resetting to Drawing on every run).

### Redesign Phase 1 (Phase 4.1) — Enterprise shell + prompt-first entry + one full design end-to-end (the smallest first-time-right win)

- **Goal:** the redesigned **shell** + **prompt-first entry** + **ONE full design end-to-end** in the refined dark studio, working first-time on the canonical **box-culvert** path: prompt-first Define (roomy gallery + "let the agent decide" + auto-detect) → live **Design** stage run (step tracker + narration, elapsed time, **NO tab-yank**) → **Overview** (verdict + key numbers from `type_summary` + drawing thumbnail + cost) → drill into Design panels (Drawing pan/zoom + Download DXF · Calc Sheet · 3D) → **Review** verdict view (verdict banner · compliance matrix · BMD/SFD + FE-agreement · proof memo). The **Stage Rail** shows Define/Design/Review live and **Simulate / Test / Approve as visibly-coming stubs**; the **Design Records rail** shows the current session's designs (basic list, replay works) with the **Projects grouping as a clearly-labelled stub**; the platform title reads **"IR Engineering Design & Proof-Check Platform"**. Everything not built in later redesign phases (full records-list filtering/status-chip breadth, per-type Overview polish across all 8 types, presets generalisation, history depth, projects) appears as a clearly-labelled stub — never a bug.
- **Scope note:** this phase reuses **every** existing backend endpoint and data field unchanged; it is a frontend re-composition of existing components into the new IA plus the two concrete fixes. Status chips render for the open design and current-session records from existing `status`+`verdict`; the *breadth* of the records list (all history, filters, dense chips) is Phase 4.2.
- **Independent slices (parallel build units):**
  - **`p4-1-shell` (frontend)** — the app shell: top bar (platform title + per-run/session token-cost split), Design Records rail (current-session records + replay + Projects stub), refined dark-studio theme. **Owns:** `frontend/src/components/AppShell.tsx`, `DesignRecordsRail.tsx`, `StatusChip.tsx`, `ProjectsStub.tsx`, `frontend/src/app/layout.tsx` (theme), `frontend/src/app/globals.css` (or Tailwind theme tokens). **Deps:** none (builds against [ui.md](ui.md) + [api.md](api.md)).
  - **`p4-1-stages` (frontend)** — the Stage Rail + stage content: `StageRail.tsx` (Define/Design/Review + Simulate/Test/Approve `⊘` stubs via `StageStub.tsx`), `OverviewPanel.tsx` (verdict + generic `type_summary` key numbers + drawing thumbnail + cost, replacing the removed Stability tab), Define stage (relocated `ComponentPicker` gallery + `PromptPanel` + `DetectedTypeChip`), Design stage (segmented `DrawingViewer`/`CalcSheet`/`Model3DViewer`), Review stage (relocated `ProofCheckPanel`). **Owns:** `frontend/src/components/StageRail.tsx`, `OverviewPanel.tsx`, `StageStub.tsx`, and edits to `page.tsx` for the no-tab-yank focus rule + platform title + removal of the culvert hero/`ArtefactTabs` six-tab bar. **Deps:** `p4-1-shell` (mounts inside `AppShell`; declared true dependency — serialise after shell's component contracts land, or build against the spec'd `AppShell` slot).
- **Key surfaces / files:** as owned above. No backend files change. `ArtefactTabs.tsx` is retired (panels move under stages); `LibraryPanel.tsx`/`TurnHistory.tsx` fold into `DesignRecordsRail.tsx`; `TypeSummaryPanel.tsx`'s generic renderer feeds `OverviewPanel.tsx`.
- **Gate command (run in order from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run pytest tests/unit tests/integration -q                       # real Gemini; backend unchanged — full suite stays green
  cd frontend && pnpm install && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line          # boots `uv run python -m src` on 8004
  ```
  The E2E asserts, on the styled dark render: (1) the top bar reads **"IR Engineering Design & Proof-Check Platform"** (no "Box Culvert"); (2) the New-design entry shows the prompt-first hero + a Civil/Mechanical gallery with "let the agent decide"; (3) submitting the canonical culvert prompt advances the Stage Rail, streams a real GA SVG in the Design → Drawing panel and a DXF download that responds, an **Overview** with a verdict + key numbers + cost, and a **Review** verdict banner + compliance matrix; (4) the **no-tab-yank** guard — after navigating to the Calc Sheet panel and triggering a refine run, the active panel is still Calc Sheet (not reset to Drawing); (5) the Simulate/Test/Approve stages and the Projects entry render as clearly-labelled `⊘ coming` stubs (present, non-error).
- **How the user tests it (handoff seed):**
  1. `cd frontend && pnpm install && pnpm build && cd ..`, then `AGENT_PORT=8004 uv run alembic upgrade head && AGENT_PORT=8004 uv run python -m src`; open `http://localhost:8004/app/`.
  2. Confirm the dark shell: top bar says **"IR Engineering Design & Proof-Check Platform"**; left rail shows Design Records with a `⊘ coming` Projects entry. Click **[+ New design]** → the prompt-first hero + roomy Civil/Mechanical gallery appear, "Let the agent decide" prominent.
  3. Submit `single box culvert, 4 m clear span, 3 m height, 2.5 m cushion, BG single line, 25t loading` → watch the Stage Rail (Define → Design → Review) and the step tracker/narration/elapsed time. The design opens on **Overview** (verdict, key FoS/capacity numbers, drawing thumbnail, cost). Open **Design → Drawing** (pan/zoom; Download DXF opens in a free viewer), **Calc Sheet** (clause-cited, expandable), **3D**. Open **Review** (verdict banner + compliance matrix + BMD/SFD + FE-agreement + memo).
  4. **No tab-yank:** while on the Calc Sheet panel, type `increase the fill to 4 m` → Refine; the run streams but you STAY on the Calc Sheet (the view is not yanked to Drawing).
  5. **Stubs (not bugs):** the Stage Rail's **Simulate / Test / Approve** and the **Projects** entry are clearly labelled "coming"; everything on the path you walked is real. Header shows per-run + session tokens/cost.

### Redesign Phase 2 (Phase 4.2) — Design Records depth: full records list · status chips · replayable history · Projects stub polish

- **Goal:** the **Design Records rail** becomes the full, first-class audit trail across ALL history (not just the current session): every design ever, newest first, each with its **status chip** (Draft / Reviewed ✓ / Needs revision ✗) derived from `status`+`verdict`, cost, component-type label and prompt summary; **replay** loads any past design's full snapshot (all stages, all artefacts) into the workspace; **history depth** — a design's own run/refine turns are browsable within its record; and the **Projects stub** is polished (a designed, clearly-labelled "coming" panel that sells the multi-design grouping vision without pretending to work). This phase makes the lifecycle records surface genuinely useful for daily work.
- **Independent slices:**
  - **`p4-2-records` (frontend)** — the full records list (reads `GET /api/designs` across sessions), status-chip derivation + rendering, replay wiring to `GET /api/designs/{run_id}`, empty/loading/error states. **Owns:** `frontend/src/components/DesignRecordsRail.tsx`, `StatusChip.tsx`, `frontend/src/lib/types.ts` (a `DesignStatusChip` derivation helper). **Deps:** none.
  - **`p4-2-history` (frontend)** — per-design run/refine history depth within the open record (browse this design's turns, replay each), and the polished Projects `⊘` stub panel. **Owns:** `frontend/src/components/DesignHistory.tsx`, `ProjectsStub.tsx`. **Deps:** none (disjoint files from `p4-2-records`).
  - **`p4-2-api-filter` (backend, OPTIONAL/thin)** — if records-list filtering needs more than today's `session_id`/`limit`/`offset`, add query params to the existing listing only. **Owns:** `src/api/designs.py` (listing query params only), `tests/unit/api/` (additions). **Deps:** none. *(Include only if the frontend genuinely needs it; otherwise this slice is dropped and the list uses existing params.)*
- **Gate command (from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run pytest tests/unit tests/integration -q
  cd frontend && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line
  ```
  E2E asserts: the records rail lists designs from more than one session with correct status chips (a `return_for_revision` run shows "Needs revision ✗", a `recommended_for_approval` run shows "Reviewed ✓"); clicking a past record replays its Overview + artefacts into the workspace; a design's run/refine history is browsable; the Projects stub renders as a clearly-labelled "coming" panel.
- **How the user tests it:** run two or three designs across sessions (culvert + retaining wall); the Design Records rail lists them all with the right status chips; click an old one — its Overview/Drawing/Calc/Review replay exactly; open its history — the refine turns are listed and each replays; the Projects entry shows a polished "coming" panel, never an error.

### Redesign Phase 3 (Phase 4.3) — Per-type Overview polish · presets generalisation · lifecycle-stub polish

- **Goal:** the redesign reaches full breadth and finish: the **Overview key-numbers** render cleanly and correctly for **all 8 component types** (culvert member-check, retaining-wall/pier stability, plate-girder stress/deflection, slab/T-beam flexure/shear, mechanical utilisation/strength/FoS) with per-type headline labelling — no blank Overview for any type; **presets are generalised** beyond the culvert (the presets surface adapts to the selected component's non-critical fields so defaults are editable per type, reusing the existing `GET/PUT /api/presets`); and the **Simulate / Test / Approve** stage stubs get a polished, presenter-ready treatment (clearly non-functional, but visually complete — the lifecycle-vision money shot). This is the finish pass that makes the studio enterprise-grade across every type.
- **Independent slices:**
  - **`p4-3-overview` (frontend)** — per-type Overview polish: extend the generic `type_summary` renderer to label/format every registered type's headline numbers, with graceful omission of any absent figure. **Owns:** `frontend/src/components/OverviewPanel.tsx`, `TypeSummaryPanel.tsx` (renderer descriptors). **Deps:** none.
  - **`p4-3-presets` (frontend + thin backend)** — generalise the presets editor to the selected component's non-critical fields (reads `GET /api/presets`, writes `PUT /api/presets/{id}`); if preset validation must span component types, extend the existing validation only. **Owns:** `frontend/src/components/PresetsEditor.tsx`, and (only if needed) `src/api/presets.py` (validation generalisation) + `tests/unit/api/`. **Deps:** none.
  - **`p4-3-stage-stubs` (frontend)** — polished Simulate/Test/Approve stage-stub panels (designed illustrations, descriptive copy). **Owns:** `frontend/src/components/StageStub.tsx`. **Deps:** none (disjoint from the other two slices).
- **Gate command (from repo root; `E2E_PORT=8004`):**
  ```bash
  uv run pytest tests/unit tests/integration -q
  cd frontend && pnpm build && cd ..
  E2E_PORT=8004 npx playwright test tests/e2e --reporter=line
  ```
  E2E asserts: a plate-girder prompt and a retaining-wall prompt each produce a non-blank Overview with correctly-labelled headline numbers (utilisation vs FoS/bearing); the presets editor shows the selected component's editable defaults and a `PUT` round-trips; the Simulate/Test/Approve stubs render their polished panels.
- **How the user tests it:** design one of each domain (a plate girder, a retaining wall, a machine element); each opens on a correct, non-blank Overview with the right headline numbers for its type; open presets for two different types and confirm each shows its own editable defaults and a saved change persists; the lifecycle stubs look presenter-ready. Nothing on the redesigned surface reads as a bug.

---

**After the redesign (not scheduled):** the actual Simulate / Test / Approve lifecycle stages (parametric sweeps, physical-test capture, formal sign-off workflow), real multi-design Projects grouping, and multi-user/auth — all beyond this POC, but the Stage Rail and Projects stub leave the seams visibly ready.
